function callAlways() {	var pathArray = window.location.pathname.split('/'),		currentPageIndex = pathArray.length-1,		currentPage = pathArray[currentPageIndex];	if(currentPage == "index.html") {		/* ==========================================================================		 CalendarIO		 ========================================================================== */		var transEndEventNames = {				'WebkitTransition' : 'webkitTransitionEnd',				'MozTransition' : 'transitionend',				'OTransition' : 'oTransitionEnd',				'msTransition' : 'MSTransitionEnd',				'transition' : 'transitionend'			},			transEndEventName = transEndEventNames[ Modernizr.prefixed( 'transition' ) ],			$wrapper = $( '#custom-inner' ),			$calendar = $( '#calendar' ),			cal = $calendar.calendario( {				onDayClick : function( $el, $contentEl, dateProperties ) {					if( $contentEl.length > 0 ) {						showEvents( $contentEl, dateProperties );					}				},				caldata : codropsEvents,				displayWeekAbbr : true			} ),			$month = $( '#custom-month' ).html( cal.getMonthName() ),			$year = $( '#custom-year' ).html( cal.getYear() );		$( '#custom-next' ).on( 'click', function() {			cal.gotoNextMonth( updateMonthYear );		} );		$( '#custom-prev' ).on( 'click', function() {			cal.gotoPreviousMonth( updateMonthYear );		} );		function updateMonthYear() {			$month.html( cal.getMonthName() );			$year.html( cal.getYear() );		}		// just an example..		function showEvents( $contentEl, dateProperties ) {			hideEvents();			var $events = $( '<div id="custom-content-reveal" class="custom-content-reveal"><h4>Events for ' + dateProperties.monthname + ' ' + dateProperties.day + ', ' + dateProperties.year + '</h4></div>' ),				$close = $( '<span class="custom-content-close"></span>' ).on( 'click', hideEvents );			$events.append( $contentEl.html() , $close ).insertAfter( $wrapper );			setTimeout( function() {				$events.css( 'top', '0%' );			}, 25 );		}		function hideEvents() {			var $events = $( '#custom-content-reveal' );			if( $events.length > 0 ) {				$events.css( 'top', '100%' );				Modernizr.csstransitions ? $events.on( transEndEventName, function() { $( this ).remove(); } ) : $events.remove();			}		}	} else if (currentPage == "appointments.html") {		$(function() {			$('#datetimepicker1').datetimepicker();		});	}}$(document).ready(function(){	callAlways();	/* ==========================================================================	 Slick	 ========================================================================== */	var $contactsSlider = $('.contacts-slider').slick({		centerMode: true,		centerPadding: '0',		slidesToShow: 3,		arrows: true,		responsive: [{			breakpoint: 400,			settings: {				centerPadding: '20px',				slidesToShow: 1			}		}, {			breakpoint: 800,			settings: {				centerPadding: '40px',				slidesToShow: 1			}		}, {			breakpoint: 900,			settings: {				centerPadding: '40px',				slidesToShow: 1			}		}]	});	$('.contacts .arrow.arrow-left').on('click', function (event) {		$contactsSlider.slick('slickPrev');	});	$('.contacts .arrow.arrow-right').on('click', function (event) {		$contactsSlider.slick('slickNext');	});});